fn main() {
    
    // ARRAYS


    let mut arr = [1, 2, 3, 4, 5];

    // Just like with tuples, arrays are defined by the types of elements inside

    arr[4] = 33;

    println!("{}", arr[4]);

    let mut _arr2: [i32; 5];             // initializing the array without values
    // So i32 array with 5 elements
    // remember that I did _arr in order not to get any warnings due to not
    // using this array.











}
